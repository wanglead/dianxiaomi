<UserControl x:Class="OrderAlert.App.Views.AccountsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="添加速卖通店铺" Command="{Binding AddAccountCommand}" Padding="14,7"/>
            <Button Content="重新登录" Command="{Binding ReloginCommand}" Padding="14,7"
                    Margin="8,0,0,0"/>
        </StackPanel>
        <DataGrid ItemsSource="{Binding Accounts}" SelectedItem="{Binding SelectedAccount}"
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                  BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="平台" Binding="{Binding Platform}" Width="130"/>
                <DataGridTextColumn Header="店铺名称" Binding="{Binding DisplayName}" Width="*"/>
                <DataGridTextColumn Header="账号标识" Binding="{Binding AccountIdentifier}" Width="180"/>
                <DataGridCheckBoxColumn Header="启用" Binding="{Binding IsEnabled}" Width="80"/>
                <DataGridTextColumn Header="上次成功检查"
                    Binding="{Binding LastSuccessfulScanAt, StringFormat={}{0:MM-dd HH:mm}}" Width="150"/>
                <DataGridTextColumn Header="最近错误" Binding="{Binding LastError}" Width="240"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
